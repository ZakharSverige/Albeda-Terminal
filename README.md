# Albeda Terminal v2.9

A lightweight desktop utility for **V Rising** players designed to enhance server discovery and automate the connection process.

## 🚀 Key Features
* **Real-time Server Browser:** Integrated with Battlemetrics API to fetch live server data.
* **Global Player Search:** Search for specific nicknames across multiple servers.
* **Auto-Connect Automation:** Automates console commands (`connect IP`) to save time.
* **Computer Vision (OpenCV):** Smart detection of loading screens to stop automation exactly when the game starts.
* **Custom UI:** Built with Pygame for a low-latency, "terminal-style" experience.

## 🛠 Tech Stack
* **Language:** Python 3.x
* **Libraries:** Pygame (UI), Requests (API), OpenCV & PyAutoGUI (Automation), Threading.

## 📖 How to Use
1. Clone the repository.
2. Install dependencies: `pip install -r requirements.txt`.
3. Run `main.py`.
4. Select a server and press **F8** to start the auto-connect bot.

---
*Note: This is a public version of the tool. Some internal analytics and private endpoints have been removed for security.*
